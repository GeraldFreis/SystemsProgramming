#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>
#include <stdlib.h>
#include <string.h>

void sequence(char *line){

}

int main(int argc, char **argv){
    int f_none_works[2];
    pid_t childprocess;
    // printf("%s", argv[1]);
        // setting up pipe
    int i=1;
    pipe(f_none_works);
    while(gets())

    return 0;
}